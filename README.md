# 1.3 – SQL Basics (DDL)

This module demonstrates SQL Data Definition Language (DDL) operations, including:

- `CREATE`, `ALTER`, and `DROP` statements
- Defining primary and foreign keys
- Applying constraints (`NOT NULL`, `UNIQUE`, `CHECK`)
- Understanding the difference between tables and views

The assignment uses **DuckDB** for local testing and verification in **DBeaver**.
